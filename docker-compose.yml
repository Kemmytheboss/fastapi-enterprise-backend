services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
        DATABASE_URL: postgresql+psycopg2://postgres:postgres@db:5432/app
        SECRET_KEY: supersecretkey
